$('#my-posts').append('<%= j render ('layouts/page_price') %>');
<% if @search_product_name_keys_paginate.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @search_product_name_keys_paginate %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>

